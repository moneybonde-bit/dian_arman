# public/

Files in this folder are served by Vite at the site root (`/`).

## cover-adat.jpg

The front cover hero image. Referenced as `/cover-adat.jpg` from
`src/data.ts` (`HERO_IMAGES`). Commit the photo here to make the
new adat cover visible.
